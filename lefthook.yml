pre-commit:
  piped: true
  commands:
    biome:
      run: npx --no-install biome check --write --staged --no-errors-on-unmatched
      stage_fixed: true
      priority: 1
    typecheck:
      run: npm run typecheck
      priority: 2
    test:
      run: npm test
      priority: 3
